#include <stdio.h>

/**
 * main - This is to print all single digit numbers in base 10
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	for (n = 0; n < 10; n++)
	{
		printf("%d", n);
	}
	printf("\n");
	return (0);
}
